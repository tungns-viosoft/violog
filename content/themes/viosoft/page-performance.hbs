{{!< default}}

{{!-- This is a page template. A page outputs content just like any other post, and has all the same
    attributes by default, but you can also customise it to behave differently if you prefer. --}}

{{!-- Everything inside the #post tags pulls data from the page --}}
{{#post}}


<script type="text/javascript" src="{{asset "js/Chart.js"}}"></script> 
<script type="text/javascript" src="{{asset "js/pechart.js"}}"></script> 



<header class="main-header post-head {{#if image}}" style="background-image: url({{image}}){{else}}no-cover{{/if}}">
    <nav class="main-nav {{#if image}}overlay{{/if}} clearfix">
        {{#if @blog.logo}}<a class="blog-logo" href="{{@blog.url}}"><img src="{{@blog.logo}}" alt="{{@blog.title}}" /></a>{{/if}}
        {{#if @blog.navigation}}
            <a class="menu-button icon-menu" href="#"><span class="word">Menu</span></a>
        {{/if}}
    </nav>
</header>

<main class="content" role="main" >

    <div class="container" ng-app="myapp" ng-controller="myctrl"  >

        <div class="row"> 
            <div class="col-md-5 srt-choose">
                <div class="srt-label">
                    <span class="label-content">SRT</span>
                </div>

                

                <div class="srt-list">
                    <select id="srt-selector" name="srt-selector" multiple >
                      <option value="volvo">Volvo</option>
                      <option value="saab">Saab</option>
                      <option value="mercedes">Mercedes</option>
                      <option value="audi">Audi</option>
                      <option value="toyota">Toyota</option>
                      <option value="porsche">Porsche</option>
                    </select>
                </div>


            </div>

        </div>


        <div class="row">
            
            <div class="col-md-8 srt-char">
                <canvas id="canvas"></canvas>
            </div>

        </div>

    </div>

    <script>



        // $("#srt-selector option").click(function(){
        //     var count = 0;
        //     $( "#srt-selector option:selected" ).each(function() {
        //       count ++;
        //     });
        //     if(count == 3){
        //         // alert("max is 2");
        //         $(this).removeAttr("selected");
        //     } else if(count > 3) {
        //         $( "#srt-selector option:selected" ).each(function() {
        //           $(this).removeAttr("selected");
        //         });
        //     }

        // });

        // $("#srt-selector").change(function(){
        //     var str = "";
        //     $( "select option:selected" ).each(function() {
        //       str += $( this ).val() + " ";
        //     });
        //     console.log(str);
        // });

        var appp = angular.module('myapp', []);

        appp.config(function($interpolateProvider) {
          $interpolateProvider.startSymbol('{[{');
          $interpolateProvider.endSymbol('}]}');
        });

        appp.controller('myctrl', function($scope, $http) {

            $http.get(
                "http://localhost:1337/api/performance"
            ).then(function(response) {

                // console.log(response);

                loadChart(response.data[0], "canvas");

            });
        });

    </script>

<!-- 
    <article class="{{post_class}}">


        <header class="post-header">
            <h1 class="post-title">{{title}}</h1>
        </header>

        <section class="post-content">
            {{content}}
        </section>

    </article>

-->

</main>





{{/post}}
